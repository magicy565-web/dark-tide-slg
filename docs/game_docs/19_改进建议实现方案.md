# 《暗潮》五大改进建议实现方案

**作者：** Manus AI
**日期：** 2026年4月4日

本文档基于《18_战国兰斯设计深度解析与暗潮改进建议.md》提出的五大改进方向，结合当前代码库的盘点结果，详细规划了具体的实现方案与新增代码结构。

## 盘点结果与实现状态

经过对现有 `.gd` 文件的全面盘点，五大改进建议的现状如下：

1.  **战果指示条（Battle Rating）**：**未实现**。目前的 `combat_resolver.gd` 仅有 12 回合后防守方胜的硬性判定，无战果条概念。
2.  **武将单回合行动限制（疲劳机制）**：**部分实现/未完善**。`game_manager.gd` 中有 `AP` 消耗机制，但缺乏明确的英雄级别疲劳标记（如 `used_this_turn`）。
3.  **角色觉醒 + 威望商店**：**部分实现**。`hero_system.gd` 有 10 级好感度（`exclusive_ending`），但无觉醒属性提升；`resource_manager.gd` 中存在 `prestige`（威望）资源，但缺乏消耗威望兑换全局增益的“深渊议会/威望商店”系统。
4.  **NG+得分自定义系统**：**未实现**。`ngplus_manager.gd` 仅提供了基于周目数的固定金币和 AP 奖励，缺乏基于“得分（Score）”的购买界面与机制。
5.  **AI军火交易与玩家破坏**：**未实现**。`diplomacy_manager.gd` 仅有基础的朝贡/同盟，无动态的资源交易与破坏机制。

---

## 具体实现方案

为了落实上述改进，我们将新增/修改以下核心系统：

### 1. 战斗系统重构：引入战果指示条

**目标文件**：`systems/combat/combat_resolver.gd` (修改)

**修改内容**：
*   在战斗状态字典 `state` 中增加 `battle_rating` 字段（初始为 50，范围 0-100）。
*   在 `_end_of_round` 或伤害结算处，根据造成的伤害比例和击杀数，动态调整 `battle_rating`。
*   修改 12 回合结束的胜负判定：若 `battle_rating > 50`，攻方胜；若 `< 50`，守方胜；若 `= 50`，平局或守方胜。

### 2. 英雄疲劳机制

**目标文件**：`systems/hero/hero_system.gd` (修改) & `autoloads/game_manager.gd` (修改)

**修改内容**：
*   在 `hero_system.gd` 中新增字典 `_hero_fatigue`，记录英雄本回合是否已行动。
*   新增方法 `set_hero_action_used(hero_id: String)` 和 `is_hero_exhausted(hero_id: String) -> bool`。
*   在回合结束信号 `turn_ended` 触发时，清空 `_hero_fatigue`。
*   在战斗结算中，疲劳的英雄若被迫防守，其 `atk` 和 `def` 属性减半。

### 3. 角色觉醒与威望商店（深渊议会）

**目标文件**：
*   `systems/hero/hero_system.gd` (修改：增加觉醒状态)
*   `systems/economy/prestige_shop.gd` (**新增**)

**新增内容**：
*   在 `hero_system.gd` 的 `get_affection_bonus` 中，Lv10 增加 `awakened = true` 标记，并给予 `all_pct = 0.20`（全属性+20%）的觉醒加成。
*   新建 `prestige_shop.gd`，管理威望兑换逻辑（如：消耗 50 威望换取 +1 AP 上限，消耗 30 威望换取招募费 -10% 等）。

### 4. NG+ 得分自定义系统

**目标文件**：`systems/ngplus/ngplus_shop.gd` (**新增**) & `systems/ngplus/ngplus_manager.gd` (修改)

**新增内容**：
*   修改 `ngplus_manager.gd`，在 `on_victory()` 时计算并保存本次通关的最终得分（Score）。
*   新建 `ngplus_shop.gd`，提供开局时的得分购买选项（如：花费 500 分继承传说武器，花费 800 分解锁特定中立势力）。

### 5. AI 动态外交与破坏

**目标文件**：`systems/faction/diplomacy_manager.gd` (修改) & `systems/faction/espionage_system.gd` (修改)

**修改内容**：
*   在 `diplomacy_manager.gd` 中新增 `process_ai_trade_routes()`，当威胁值 > 30 时，光明阵营之间建立资源输送（如人类给法师送铁）。
*   在 `espionage_system.gd` 中新增玩家破坏交易路线的间谍行动，成功可截获资源并降低敌方结盟效率。

---

## 实施里程碑（补完）

> 以下里程碑用于将本方案从“设计建议”推进到“可验收功能”，并与当前 Godot 项目迭代节奏对齐。

### M1：战斗可视反馈与判定一致性（预计 2~3 天）

**范围**：
1. `combat_resolver.gd` 增加 `battle_rating` 状态字段；
2. 战斗每回合结算后触发 `battle_rating_changed(attacker, defender, rating)` 事件；
3. HUD 新增战果条组件，显示 0~100 的实时倾向；
4. 12 回合终局判定改为读取战果条。

**验收标准**：
- 固定随机种子下，重复战斗 20 次，终局判定结果与战果条方向一致率 100%；
- UI 不出现空引用报错（特别是自动战斗、跳过战斗流程）；
- 日志中可追踪每回合战果变化来源（伤害、击杀、士气）。

### M2：英雄疲劳与行动经济（预计 1~2 天）

**范围**：
1. `hero_system.gd` 引入 `_hero_fatigue: Dictionary`；
2. `game_manager.gd` 在英雄执行主动动作后标记疲劳；
3. 回合切换时统一清空疲劳；
4. 疲劳英雄被动参战时套用衰减（建议 `atk/def -40%`，可配表）。

**验收标准**：
- 同一英雄单回合无法重复触发“主动出击”类收益；
- 回合结束后疲劳状态完全重置；
- 存档/读档后疲劳状态不串回合（仅保存当前回合实时状态）。

### M3：威望商店 + 觉醒（预计 2~4 天）

**范围**：
1. 新增 `systems/economy/prestige_shop.gd`（纯逻辑）；
2. 新增 `data/prestige_shop_items.json`（可热更配置）；
3. `hero_system.gd` 接入觉醒状态与属性加成；
4. UI 增加“深渊议会”入口（仅在满足主线章节后开放）。

**验收标准**：
- 威望点数扣减与增益生效完全一致（无重复购买漏洞）；
- 觉醒状态可在角色面板和战斗预览中正确展示；
- 非法配置（负价格、未知效果）会被加载校验拦截并告警。

### M4：NG+ 得分商店（预计 2~3 天）

**范围**：
1. `ngplus_manager.gd` 结算阶段输出最终 Score；
2. 新增 `systems/ngplus/ngplus_shop.gd`；
3. 开局流程接入“得分消费→开局奖励注入”；
4. 增加一次性奖励与可叠加奖励两类商品。

**验收标准**：
- 单局得分计算可复现（同存档、同结局一致）；
- 商店购买不会越权写入非法资源字段；
- NG+ 奖励链路覆盖“新开档/读档继续/重开”三类入口。

### M5：AI 交易与可破坏供应链（预计 3~5 天）

**范围**：
1. `diplomacy_manager.gd` 增加 AI 交易路线生成与维护；
2. `espionage_system.gd` 增加“破坏路线”行动；
3. 地图层显示关键贸易线（可选简化模式）；
4. 接入威胁值/关系值对交易意愿和防护等级的影响。

**验收标准**：
- AI 能在资源紧缺时优先建立补给路线；
- 玩家破坏后可观测到敌方资源波动与外交态度变化；
- 每回合 AI 处理成本受控（大地图不出现明显卡顿）。

---

## 技术拆分建议（避免大改一次性爆炸）

1. **先做“纯逻辑 + 事件”再做 UI**：每个里程碑先实现可测试逻辑层，再补显示层；
2. **配置优先**：新系统优先使用 JSON/常量配置，减少硬编码；
3. **存档兼容先行**：凡新增状态字段，必须同步补齐版本迁移；
4. **灰度启用**：以 `FeatureFlags`（如 `enable_battle_rating`）控制上线范围，便于回滚。

---

## 测试清单（文档补完）

### 自动化建议
- 单元测试：`get_action_ap_cost`、战果计算、疲劳状态机、商店购买边界；
- 集成测试：完整回合流程（内政→战斗→外交→回合结算）；
- 回归测试：存档读档、教程关卡、AI 回合性能。

### 手工验收建议
- 新手局（前 10 回合）：确认系统提示清晰，不造成学习门槛陡增；
- 中盘局（20~40 回合）：确认战果条和 AI 交易带来可感知策略差异；
- 终盘局（70% 领土后）：确认外交与投降机制不会被新系统破坏。

---

## 下一步行动（执行顺序）

1. 先落地 **M1 + M2**（最小可玩闭环，直接提升战斗反馈与规则一致性）；
2. 再推进 **M3 + M4**（成长与周目动力）；
3. 最后上线 **M5**（系统复杂度最高，依赖前置数据稳定）；
4. 每个里程碑完成后，更新本文件“实现状态”与“实际偏差”记录，形成可追踪迭代日志。
